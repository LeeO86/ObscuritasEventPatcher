// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: github.com/openconfig/gnmi/proto/gnmi/gnmi.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type ClientUnaryCall,
  type handleBidiStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Any } from "../../../../../google/protobuf/any";
import { Extension } from "../gnmi_ext/gnmi_ext";

export const protobufPackage = "gnmi";

/**
 * Copyright 2016 Google Inc. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * Encoding defines the value encoding formats that are supported by the gNMI
 * protocol. These encodings are used by both the client (when sending Set
 * messages to modify the state of the target) and the target when serializing
 * data to be returned to the client (in both Subscribe and Get RPCs).
 * Reference: gNMI Specification Section 2.3
 */
export enum Encoding {
  /** JSON - JSON encoded text. */
  JSON = 0,
  /** BYTES - Arbitrarily encoded bytes. */
  BYTES = 1,
  /** PROTO - Encoded according to scalar values of TypedValue. */
  PROTO = 2,
  /** ASCII - ASCII text of an out-of-band agreed format. */
  ASCII = 3,
  /** JSON_IETF - JSON encoded text as per RFC7951. */
  JSON_IETF = 4,
  UNRECOGNIZED = -1,
}

export function encodingFromJSON(object: any): Encoding {
  switch (object) {
    case 0:
    case "JSON":
      return Encoding.JSON;
    case 1:
    case "BYTES":
      return Encoding.BYTES;
    case 2:
    case "PROTO":
      return Encoding.PROTO;
    case 3:
    case "ASCII":
      return Encoding.ASCII;
    case 4:
    case "JSON_IETF":
      return Encoding.JSON_IETF;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Encoding.UNRECOGNIZED;
  }
}

export function encodingToJSON(object: Encoding): string {
  switch (object) {
    case Encoding.JSON:
      return "JSON";
    case Encoding.BYTES:
      return "BYTES";
    case Encoding.PROTO:
      return "PROTO";
    case Encoding.ASCII:
      return "ASCII";
    case Encoding.JSON_IETF:
      return "JSON_IETF";
    case Encoding.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * SubscriptionMode is the mode of the subscription, specifying how the
 * target must return values in a subscription.
 * Reference: gNMI Specification Section 3.5.1.3
 */
export enum SubscriptionMode {
  /** TARGET_DEFINED - The target selects the relevant mode for each element. */
  TARGET_DEFINED = 0,
  /** ON_CHANGE - The target sends an update on element value change. */
  ON_CHANGE = 1,
  /** SAMPLE - The target samples values according to the interval. */
  SAMPLE = 2,
  UNRECOGNIZED = -1,
}

export function subscriptionModeFromJSON(object: any): SubscriptionMode {
  switch (object) {
    case 0:
    case "TARGET_DEFINED":
      return SubscriptionMode.TARGET_DEFINED;
    case 1:
    case "ON_CHANGE":
      return SubscriptionMode.ON_CHANGE;
    case 2:
    case "SAMPLE":
      return SubscriptionMode.SAMPLE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SubscriptionMode.UNRECOGNIZED;
  }
}

export function subscriptionModeToJSON(object: SubscriptionMode): string {
  switch (object) {
    case SubscriptionMode.TARGET_DEFINED:
      return "TARGET_DEFINED";
    case SubscriptionMode.ON_CHANGE:
      return "ON_CHANGE";
    case SubscriptionMode.SAMPLE:
      return "SAMPLE";
    case SubscriptionMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Notification is a re-usable message that is used to encode data from the
 * target to the client. A Notification carries two types of changes to the data
 * tree:
 *  - Deleted values (delete) - a set of paths that have been removed from the
 *    data tree.
 *  - Updated values (update) - a set of path-value pairs indicating the path
 *    whose value has changed in the data tree.
 * Reference: gNMI Specification Section 2.1
 */
export interface Notification {
  /** Timestamp in nanoseconds since Epoch. */
  timestamp: string;
  /** Prefix used for paths in the message. */
  prefix?:
    | Path
    | undefined;
  /** Data elements that have changed values. */
  update: Update[];
  /** Data elements that have been deleted. */
  delete: Path[];
  /**
   * This notification contains a set of paths that are always updated together
   * referenced by a globally unique prefix.
   */
  atomic: boolean;
}

/**
 * Update is a re-usable message that is used to store a particular Path,
 * Value pair.
 * Reference: gNMI Specification Section 2.1
 */
export interface Update {
  /** The path (key) for the update. */
  path?:
    | Path
    | undefined;
  /**
   * The value (value) for the update.
   *
   * @deprecated
   */
  value?:
    | Value
    | undefined;
  /** The explicitly typed update value. */
  val?:
    | TypedValue
    | undefined;
  /** Number of coalesced duplicates. */
  duplicates: number;
}

/**
 * TypedValue is used to encode a value being sent between the client and
 * target (originated by either entity).
 */
export interface TypedValue {
  /** String value. */
  stringVal?:
    | string
    | undefined;
  /** Integer value. */
  intVal?:
    | string
    | undefined;
  /** Unsigned integer value. */
  uintVal?:
    | string
    | undefined;
  /** Bool value. */
  boolVal?:
    | boolean
    | undefined;
  /** Arbitrary byte sequence value. */
  bytesVal?:
    | Buffer
    | undefined;
  /**
   * Deprecated - use double_val.
   *
   * @deprecated
   */
  floatVal?:
    | number
    | undefined;
  /** Floating point value. */
  doubleVal?:
    | number
    | undefined;
  /**
   * Deprecated - use double_val.
   *
   * @deprecated
   */
  decimalVal?:
    | Decimal64
    | undefined;
  /** Mixed type scalar array value. */
  leaflistVal?:
    | ScalarArray
    | undefined;
  /** protobuf.Any encoded bytes. */
  anyVal?:
    | Any
    | undefined;
  /** JSON-encoded text. */
  jsonVal?:
    | Buffer
    | undefined;
  /** JSON-encoded text per RFC7951. */
  jsonIetfVal?:
    | Buffer
    | undefined;
  /** Arbitrary ASCII text. */
  asciiVal?:
    | string
    | undefined;
  /**
   * Protobuf binary encoded bytes. The message type is not included.
   * See the specification at
   * github.com/openconfig/reference/blob/master/rpc/gnmi/protobuf-vals.md
   * for a complete specification. [Experimental]
   */
  protoBytes?: Buffer | undefined;
}

/**
 * Path encodes a data tree path as a series of repeated strings, with
 * each element of the path representing a data tree node name and the
 * associated attributes.
 * Reference: gNMI Specification Section 2.2.2.
 */
export interface Path {
  /**
   * Elements of the path are no longer encoded as a string, but rather within
   * the elem field as a PathElem message.
   *
   * @deprecated
   */
  element: string[];
  /** Label to disambiguate path. */
  origin: string;
  /** Elements of the path. */
  elem: PathElem[];
  /** The name of the target */
  target: string;
}

/**
 * PathElem encodes an element of a gNMI path, along with any attributes (keys)
 * that may be associated with it.
 * Reference: gNMI Specification Section 2.2.2.
 */
export interface PathElem {
  /** The name of the element in the path. */
  name: string;
  /** Map of key (attribute) name to value. */
  key: { [key: string]: string };
}

export interface PathElem_KeyEntry {
  key: string;
  value: string;
}

/**
 * Value encodes a data tree node's value - along with the way in which
 * the value is encoded. This message is deprecated by gNMI 0.3.0.
 * Reference: gNMI Specification Section 2.2.3.
 *
 * @deprecated
 */
export interface Value {
  /** Value of the variable being transmitted. */
  value: Buffer;
  /** Encoding used for the value field. */
  type: Encoding;
}

/**
 * Error message previously utilised to return errors to the client. Deprecated
 * in favour of using the google.golang.org/genproto/googleapis/rpc/status
 * message in the RPC response.
 * Reference: gNMI Specification Section 2.5
 *
 * @deprecated
 */
export interface Error {
  /** Canonical gRPC error code. */
  code: number;
  /** Human readable error. */
  message: string;
  /** Optional additional information. */
  data?: Any | undefined;
}

/**
 * Decimal64 is used to encode a fixed precision decimal number. The value
 * is expressed as a set of digits with the precision specifying the
 * number of digits following the decimal point in the digit set.
 * This message is deprecated in favor of encoding all floating point types
 * as double precision.
 *
 * @deprecated
 */
export interface Decimal64 {
  /** Set of digits. */
  digits: string;
  /** Number of digits following the decimal point. */
  precision: number;
}

/** ScalarArray is used to encode a mixed-type array of values. */
export interface ScalarArray {
  /**
   * The set of elements within the array. Each TypedValue message should
   * specify only elements that have a field identifier of 1-7 (i.e., the
   * values are scalar values).
   */
  element: TypedValue[];
}

/**
 * SubscribeRequest is the message sent by the client to the target when
 * initiating a subscription to a set of paths within the data tree. The
 * request field must be populated and the initial message must specify a
 * SubscriptionList to initiate a subscription.
 * Reference: gNMI Specification Section 3.5.1.1
 */
export interface SubscribeRequest {
  /** Specify the paths within a subscription. */
  subscribe?:
    | SubscriptionList
    | undefined;
  /** Trigger a polled update. */
  poll?:
    | Poll
    | undefined;
  /**
   * Extension messages associated with the SubscribeRequest. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * Poll is sent within a SubscribeRequest to trigger the device to
 * send telemetry updates for the paths that are associated with the
 * subscription.
 * Reference: gNMI Specification Section Section 3.5.1.4
 */
export interface Poll {
}

/**
 * SubscribeResponse is the message used by the target within a Subscribe RPC.
 * The target includes a Notification message which is used to transmit values
 * of the path(s) that are associated with the subscription. The same message
 * is to indicate that the target has sent all data values once (is
 * synchronized).
 * Reference: gNMI Specification Section 3.5.1.4
 */
export interface SubscribeResponse {
  /** Changed or sampled value for a path. */
  update?:
    | Notification
    | undefined;
  /**
   * Indicate target has sent all values associated with the subscription
   * at least once.
   */
  syncResponse?:
    | boolean
    | undefined;
  /**
   * Deprecated in favour of google.golang.org/genproto/googleapis/rpc/status
   *
   * @deprecated
   */
  error?:
    | Error
    | undefined;
  /**
   * Extension messages associated with the SubscribeResponse. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * SubscriptionList is used within a Subscribe message to specify the list of
 * paths that the client wishes to subscribe to. The message consists of a
 * list of (possibly prefixed) paths, and options that relate to the
 * subscription.
 * Reference: gNMI Specification Section 3.5.1.2
 */
export interface SubscriptionList {
  /** Prefix used for paths. */
  prefix?:
    | Path
    | undefined;
  /** Set of subscriptions to create. */
  subscription: Subscription[];
  /** DSCP marking to be used. */
  qos?: QOSMarking | undefined;
  mode: SubscriptionList_Mode;
  /**
   * Whether elements of the schema that are marked as eligible for aggregation
   * should be aggregated or not.
   */
  allowAggregation: boolean;
  /**
   * The set of schemas that define the elements of the data tree that should
   * be sent by the target.
   */
  useModels: ModelData[];
  /**
   * The encoding that the target should use within the Notifications generated
   * corresponding to the SubscriptionList.
   */
  encoding: Encoding;
  /**
   * An optional field to specify that only updates to current state should be
   * sent to a client. If set, the initial state is not sent to the client but
   * rather only the sync message followed by any subsequent updates to the
   * current state. For ONCE and POLL modes, this causes the server to send only
   * the sync message (Sec. 3.5.2.3).
   */
  updatesOnly: boolean;
}

/** Mode of the subscription. */
export enum SubscriptionList_Mode {
  /** STREAM - Values streamed by the target (Sec. 3.5.1.5.2). */
  STREAM = 0,
  /** ONCE - Values sent once-off by the target (Sec. 3.5.1.5.1). */
  ONCE = 1,
  /** POLL - Values sent in response to a poll request (Sec. 3.5.1.5.3). */
  POLL = 2,
  UNRECOGNIZED = -1,
}

export function subscriptionList_ModeFromJSON(object: any): SubscriptionList_Mode {
  switch (object) {
    case 0:
    case "STREAM":
      return SubscriptionList_Mode.STREAM;
    case 1:
    case "ONCE":
      return SubscriptionList_Mode.ONCE;
    case 2:
    case "POLL":
      return SubscriptionList_Mode.POLL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SubscriptionList_Mode.UNRECOGNIZED;
  }
}

export function subscriptionList_ModeToJSON(object: SubscriptionList_Mode): string {
  switch (object) {
    case SubscriptionList_Mode.STREAM:
      return "STREAM";
    case SubscriptionList_Mode.ONCE:
      return "ONCE";
    case SubscriptionList_Mode.POLL:
      return "POLL";
    case SubscriptionList_Mode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Subscription is a single request within a SubscriptionList. The path
 * specified is interpreted (along with the prefix) as the elements of the data
 * tree that the client is subscribing to. The mode determines how the target
 * should trigger updates to be sent.
 * Reference: gNMI Specification Section 3.5.1.3
 */
export interface Subscription {
  /** The data tree path. */
  path?:
    | Path
    | undefined;
  /** Subscription mode to be used. */
  mode: SubscriptionMode;
  /** ns between samples in SAMPLE mode. */
  sampleInterval: string;
  /**
   * Indicates whether values that have not changed should be sent in a SAMPLE
   * subscription.
   */
  suppressRedundant: boolean;
  /**
   * 1. A heartbeat interval MAY be specified along with an “on change”
   * subscription - in this case, the value of the data item(s) MUST be re-sent
   * once per heartbeat interval regardless of whether the value has changed or
   * not.
   * 2. A heartbeat_interval MAY be specified to modify the behavior of
   * suppress_redundant in a sampled subscription. In this case, the
   * target MUST generate one telemetry update per heartbeat interval,
   * regardless of whether the suppress_redundant flag is set to true.
   * This value is specified as an unsigned 64-bit integer in nanoseconds
   */
  heartbeatInterval: string;
}

/**
 * QOSMarking specifies the DSCP value to be set on transmitted telemetry
 * updates from the target.
 * Reference: gNMI Specification Section 3.5.1.2
 */
export interface QOSMarking {
  marking: number;
}

/**
 * SetRequest is sent from a client to the target to update values in the data
 * tree. Paths are either deleted by the client, or modified by means of being
 * updated, or replaced. Where a replace is used, unspecified values are
 * considered to be replaced, whereas when update is used the changes are
 * considered to be incremental. The set of changes that are specified within
 * a single SetRequest are considered to be a transaction.
 * Reference: gNMI Specification Section 3.4.1
 */
export interface SetRequest {
  /** Prefix used for paths in the message. */
  prefix?:
    | Path
    | undefined;
  /** Paths to be deleted from the data tree. */
  delete: Path[];
  /** Updates specifying elements to be replaced. */
  replace: Update[];
  /** Updates specifying elements to updated. */
  update: Update[];
  /**
   * Updates specifying elements to union and then replace the data tree.
   * See the gNMI specification at
   * https://github.com/openconfig/reference/blob/master/rpc/gnmi/gnmi-specification.md
   * for details.
   */
  unionReplace: Update[];
  /**
   * Extension messages associated with the SetRequest. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * SetResponse is the response to a SetRequest, sent from the target to the
 * client. It reports the result of the modifications to the data tree that were
 * specified by the client. Errors for this RPC should be reported using the
 * https://github.com/googleapis/googleapis/blob/master/google/rpc/status.proto
 * message in the RPC return. The gnmi.Error message can be used to add
 * additional details where required. Reference: gNMI Specification
 * Section 3.4.2
 */
export interface SetResponse {
  /** Prefix used for paths. */
  prefix?:
    | Path
    | undefined;
  /**
   * A set of responses specifying the result of the operations specified in
   * the SetRequest.
   */
  response: UpdateResult[];
  /**
   * The overall status of the transaction.
   *
   * @deprecated
   */
  message?:
    | Error
    | undefined;
  /** Timestamp of transaction (ns since epoch). */
  timestamp: string;
  /**
   * Extension messages associated with the SetResponse. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * UpdateResult is used within the SetResponse message to communicate the
 * result of an operation specified within a SetRequest message.
 * Reference: gNMI Specification Section 3.4.2
 */
export interface UpdateResult {
  /**
   * Deprecated timestamp for the UpdateResult, this field has been
   * replaced by the timestamp within the SetResponse message, since
   * all mutations effected by a set should be applied as a single
   * transaction.
   *
   * @deprecated
   */
  timestamp: string;
  /** Path associated with the update. */
  path?:
    | Path
    | undefined;
  /**
   * Status of the update operation.
   *
   * @deprecated
   */
  message?:
    | Error
    | undefined;
  /** Update operation type. */
  op: UpdateResult_Operation;
}

/** The operation that was associated with the Path specified. */
export enum UpdateResult_Operation {
  INVALID = 0,
  /** DELETE - The result relates to a delete of Path. */
  DELETE = 1,
  /** REPLACE - The result relates to a replace of Path. */
  REPLACE = 2,
  /** UPDATE - The result relates to an update of Path. */
  UPDATE = 3,
  /** UNION_REPLACE - The result of a union_replace of Path or CLI origin. */
  UNION_REPLACE = 4,
  UNRECOGNIZED = -1,
}

export function updateResult_OperationFromJSON(object: any): UpdateResult_Operation {
  switch (object) {
    case 0:
    case "INVALID":
      return UpdateResult_Operation.INVALID;
    case 1:
    case "DELETE":
      return UpdateResult_Operation.DELETE;
    case 2:
    case "REPLACE":
      return UpdateResult_Operation.REPLACE;
    case 3:
    case "UPDATE":
      return UpdateResult_Operation.UPDATE;
    case 4:
    case "UNION_REPLACE":
      return UpdateResult_Operation.UNION_REPLACE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return UpdateResult_Operation.UNRECOGNIZED;
  }
}

export function updateResult_OperationToJSON(object: UpdateResult_Operation): string {
  switch (object) {
    case UpdateResult_Operation.INVALID:
      return "INVALID";
    case UpdateResult_Operation.DELETE:
      return "DELETE";
    case UpdateResult_Operation.REPLACE:
      return "REPLACE";
    case UpdateResult_Operation.UPDATE:
      return "UPDATE";
    case UpdateResult_Operation.UNION_REPLACE:
      return "UNION_REPLACE";
    case UpdateResult_Operation.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * GetRequest is sent when a client initiates a Get RPC. It is used to specify
 * the set of data elements for which the target should return a snapshot of
 * data. The use_models field specifies the set of schema modules that are to
 * be used by the target - where use_models is not specified then the target
 * must use all schema models that it has.
 * Reference: gNMI Specification Section 3.3.1
 */
export interface GetRequest {
  /** Prefix used for paths. */
  prefix?:
    | Path
    | undefined;
  /** Paths requested by the client. */
  path: Path[];
  /** The type of data being requested. */
  type: GetRequest_DataType;
  /** Encoding to be used. */
  encoding: Encoding;
  /** The schema models to be used. */
  useModels: ModelData[];
  /**
   * Extension messages associated with the GetRequest. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/** Type of elements within the data tree. */
export enum GetRequest_DataType {
  /** ALL - All data elements. */
  ALL = 0,
  /** CONFIG - Config (rw) only elements. */
  CONFIG = 1,
  /** STATE - State (ro) only elements. */
  STATE = 2,
  /**
   * OPERATIONAL - Data elements marked in the schema as operational. This refers to data
   * elements whose value relates to the state of processes or interactions
   * running on the device.
   */
  OPERATIONAL = 3,
  UNRECOGNIZED = -1,
}

export function getRequest_DataTypeFromJSON(object: any): GetRequest_DataType {
  switch (object) {
    case 0:
    case "ALL":
      return GetRequest_DataType.ALL;
    case 1:
    case "CONFIG":
      return GetRequest_DataType.CONFIG;
    case 2:
    case "STATE":
      return GetRequest_DataType.STATE;
    case 3:
    case "OPERATIONAL":
      return GetRequest_DataType.OPERATIONAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return GetRequest_DataType.UNRECOGNIZED;
  }
}

export function getRequest_DataTypeToJSON(object: GetRequest_DataType): string {
  switch (object) {
    case GetRequest_DataType.ALL:
      return "ALL";
    case GetRequest_DataType.CONFIG:
      return "CONFIG";
    case GetRequest_DataType.STATE:
      return "STATE";
    case GetRequest_DataType.OPERATIONAL:
      return "OPERATIONAL";
    case GetRequest_DataType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * GetResponse is used by the target to respond to a GetRequest from a client.
 * The set of Notifications corresponds to the data values that are requested
 * by the client in the GetRequest.
 * Reference: gNMI Specification Section 3.3.2
 */
export interface GetResponse {
  /** Data values. */
  notification: Notification[];
  /**
   * Errors that occurred in the Get.
   *
   * @deprecated
   */
  error?:
    | Error
    | undefined;
  /**
   * Extension messages associated with the GetResponse. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * CapabilityRequest is sent by the client in the Capabilities RPC to request
 * that the target reports its capabilities.
 * Reference: gNMI Specification Section 3.2.1
 */
export interface CapabilityRequest {
  /**
   * Extension messages associated with the CapabilityRequest. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * CapabilityResponse is used by the target to report its capabilities to the
 * client within the Capabilities RPC.
 * Reference: gNMI Specification Section 3.2.2
 */
export interface CapabilityResponse {
  /** Supported schema models. */
  supportedModels: ModelData[];
  /** Supported encodings. */
  supportedEncodings: Encoding[];
  /** Supported gNMI version. */
  gNMIVersion: string;
  /**
   * Extension messages associated with the CapabilityResponse. See the
   * gNMI extension specification for further definition.
   */
  extension: Extension[];
}

/**
 * ModelData is used to describe a set of schema modules. It can be used in a
 * CapabilityResponse where a target reports the set of modules that it
 * supports, and within the SubscribeRequest and GetRequest messages to specify
 * the set of models from which data tree elements should be reported.
 * Reference: gNMI Specification Section 3.2.3
 */
export interface ModelData {
  /** Name of the model. */
  name: string;
  /** Organization publishing the model. */
  organization: string;
  /** Semantic version of the model. */
  version: string;
}

function createBaseNotification(): Notification {
  return { timestamp: "0", prefix: undefined, update: [], delete: [], atomic: false };
}

export const Notification: MessageFns<Notification> = {
  encode(message: Notification, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== "0") {
      writer.uint32(8).int64(message.timestamp);
    }
    if (message.prefix !== undefined) {
      Path.encode(message.prefix, writer.uint32(18).fork()).join();
    }
    for (const v of message.update) {
      Update.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.delete) {
      Path.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.atomic !== false) {
      writer.uint32(48).bool(message.atomic);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Notification {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotification();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.prefix = Path.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.update.push(Update.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.delete.push(Path.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.atomic = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Notification {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "0",
      prefix: isSet(object.prefix) ? Path.fromJSON(object.prefix) : undefined,
      update: globalThis.Array.isArray(object?.update) ? object.update.map((e: any) => Update.fromJSON(e)) : [],
      delete: globalThis.Array.isArray(object?.delete) ? object.delete.map((e: any) => Path.fromJSON(e)) : [],
      atomic: isSet(object.atomic) ? globalThis.Boolean(object.atomic) : false,
    };
  },

  toJSON(message: Notification): unknown {
    const obj: any = {};
    if (message.timestamp !== "0") {
      obj.timestamp = message.timestamp;
    }
    if (message.prefix !== undefined) {
      obj.prefix = Path.toJSON(message.prefix);
    }
    if (message.update?.length) {
      obj.update = message.update.map((e) => Update.toJSON(e));
    }
    if (message.delete?.length) {
      obj.delete = message.delete.map((e) => Path.toJSON(e));
    }
    if (message.atomic !== false) {
      obj.atomic = message.atomic;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Notification>, I>>(base?: I): Notification {
    return Notification.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Notification>, I>>(object: I): Notification {
    const message = createBaseNotification();
    message.timestamp = object.timestamp ?? "0";
    message.prefix = (object.prefix !== undefined && object.prefix !== null)
      ? Path.fromPartial(object.prefix)
      : undefined;
    message.update = object.update?.map((e) => Update.fromPartial(e)) || [];
    message.delete = object.delete?.map((e) => Path.fromPartial(e)) || [];
    message.atomic = object.atomic ?? false;
    return message;
  },
};

function createBaseUpdate(): Update {
  return { path: undefined, value: undefined, val: undefined, duplicates: 0 };
}

export const Update: MessageFns<Update> = {
  encode(message: Update, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      Path.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.value !== undefined) {
      Value.encode(message.value, writer.uint32(18).fork()).join();
    }
    if (message.val !== undefined) {
      TypedValue.encode(message.val, writer.uint32(26).fork()).join();
    }
    if (message.duplicates !== 0) {
      writer.uint32(32).uint32(message.duplicates);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Update {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = Value.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.val = TypedValue.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.duplicates = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Update {
    return {
      path: isSet(object.path) ? Path.fromJSON(object.path) : undefined,
      value: isSet(object.value) ? Value.fromJSON(object.value) : undefined,
      val: isSet(object.val) ? TypedValue.fromJSON(object.val) : undefined,
      duplicates: isSet(object.duplicates) ? globalThis.Number(object.duplicates) : 0,
    };
  },

  toJSON(message: Update): unknown {
    const obj: any = {};
    if (message.path !== undefined) {
      obj.path = Path.toJSON(message.path);
    }
    if (message.value !== undefined) {
      obj.value = Value.toJSON(message.value);
    }
    if (message.val !== undefined) {
      obj.val = TypedValue.toJSON(message.val);
    }
    if (message.duplicates !== 0) {
      obj.duplicates = Math.round(message.duplicates);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Update>, I>>(base?: I): Update {
    return Update.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Update>, I>>(object: I): Update {
    const message = createBaseUpdate();
    message.path = (object.path !== undefined && object.path !== null) ? Path.fromPartial(object.path) : undefined;
    message.value = (object.value !== undefined && object.value !== null) ? Value.fromPartial(object.value) : undefined;
    message.val = (object.val !== undefined && object.val !== null) ? TypedValue.fromPartial(object.val) : undefined;
    message.duplicates = object.duplicates ?? 0;
    return message;
  },
};

function createBaseTypedValue(): TypedValue {
  return {
    stringVal: undefined,
    intVal: undefined,
    uintVal: undefined,
    boolVal: undefined,
    bytesVal: undefined,
    floatVal: undefined,
    doubleVal: undefined,
    decimalVal: undefined,
    leaflistVal: undefined,
    anyVal: undefined,
    jsonVal: undefined,
    jsonIetfVal: undefined,
    asciiVal: undefined,
    protoBytes: undefined,
  };
}

export const TypedValue: MessageFns<TypedValue> = {
  encode(message: TypedValue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stringVal !== undefined) {
      writer.uint32(10).string(message.stringVal);
    }
    if (message.intVal !== undefined) {
      writer.uint32(16).int64(message.intVal);
    }
    if (message.uintVal !== undefined) {
      writer.uint32(24).uint64(message.uintVal);
    }
    if (message.boolVal !== undefined) {
      writer.uint32(32).bool(message.boolVal);
    }
    if (message.bytesVal !== undefined) {
      writer.uint32(42).bytes(message.bytesVal);
    }
    if (message.floatVal !== undefined) {
      writer.uint32(53).float(message.floatVal);
    }
    if (message.doubleVal !== undefined) {
      writer.uint32(113).double(message.doubleVal);
    }
    if (message.decimalVal !== undefined) {
      Decimal64.encode(message.decimalVal, writer.uint32(58).fork()).join();
    }
    if (message.leaflistVal !== undefined) {
      ScalarArray.encode(message.leaflistVal, writer.uint32(66).fork()).join();
    }
    if (message.anyVal !== undefined) {
      Any.encode(message.anyVal, writer.uint32(74).fork()).join();
    }
    if (message.jsonVal !== undefined) {
      writer.uint32(82).bytes(message.jsonVal);
    }
    if (message.jsonIetfVal !== undefined) {
      writer.uint32(90).bytes(message.jsonIetfVal);
    }
    if (message.asciiVal !== undefined) {
      writer.uint32(98).string(message.asciiVal);
    }
    if (message.protoBytes !== undefined) {
      writer.uint32(106).bytes(message.protoBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TypedValue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTypedValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stringVal = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.intVal = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.uintVal = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.boolVal = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.bytesVal = Buffer.from(reader.bytes());
          continue;
        }
        case 6: {
          if (tag !== 53) {
            break;
          }

          message.floatVal = reader.float();
          continue;
        }
        case 14: {
          if (tag !== 113) {
            break;
          }

          message.doubleVal = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.decimalVal = Decimal64.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.leaflistVal = ScalarArray.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.anyVal = Any.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.jsonVal = Buffer.from(reader.bytes());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.jsonIetfVal = Buffer.from(reader.bytes());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.asciiVal = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.protoBytes = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TypedValue {
    return {
      stringVal: isSet(object.stringVal)
        ? globalThis.String(object.stringVal)
        : isSet(object.string_val)
        ? globalThis.String(object.string_val)
        : undefined,
      intVal: isSet(object.intVal)
        ? globalThis.String(object.intVal)
        : isSet(object.int_val)
        ? globalThis.String(object.int_val)
        : undefined,
      uintVal: isSet(object.uintVal)
        ? globalThis.String(object.uintVal)
        : isSet(object.uint_val)
        ? globalThis.String(object.uint_val)
        : undefined,
      boolVal: isSet(object.boolVal)
        ? globalThis.Boolean(object.boolVal)
        : isSet(object.bool_val)
        ? globalThis.Boolean(object.bool_val)
        : undefined,
      bytesVal: isSet(object.bytesVal)
        ? Buffer.from(bytesFromBase64(object.bytesVal))
        : isSet(object.bytes_val)
        ? Buffer.from(bytesFromBase64(object.bytes_val))
        : undefined,
      floatVal: isSet(object.floatVal)
        ? globalThis.Number(object.floatVal)
        : isSet(object.float_val)
        ? globalThis.Number(object.float_val)
        : undefined,
      doubleVal: isSet(object.doubleVal)
        ? globalThis.Number(object.doubleVal)
        : isSet(object.double_val)
        ? globalThis.Number(object.double_val)
        : undefined,
      decimalVal: isSet(object.decimalVal)
        ? Decimal64.fromJSON(object.decimalVal)
        : isSet(object.decimal_val)
        ? Decimal64.fromJSON(object.decimal_val)
        : undefined,
      leaflistVal: isSet(object.leaflistVal)
        ? ScalarArray.fromJSON(object.leaflistVal)
        : isSet(object.leaflist_val)
        ? ScalarArray.fromJSON(object.leaflist_val)
        : undefined,
      anyVal: isSet(object.anyVal)
        ? Any.fromJSON(object.anyVal)
        : isSet(object.any_val)
        ? Any.fromJSON(object.any_val)
        : undefined,
      jsonVal: isSet(object.jsonVal)
        ? Buffer.from(bytesFromBase64(object.jsonVal))
        : isSet(object.json_val)
        ? Buffer.from(bytesFromBase64(object.json_val))
        : undefined,
      jsonIetfVal: isSet(object.jsonIetfVal)
        ? Buffer.from(bytesFromBase64(object.jsonIetfVal))
        : isSet(object.json_ietf_val)
        ? Buffer.from(bytesFromBase64(object.json_ietf_val))
        : undefined,
      asciiVal: isSet(object.asciiVal)
        ? globalThis.String(object.asciiVal)
        : isSet(object.ascii_val)
        ? globalThis.String(object.ascii_val)
        : undefined,
      protoBytes: isSet(object.protoBytes)
        ? Buffer.from(bytesFromBase64(object.protoBytes))
        : isSet(object.proto_bytes)
        ? Buffer.from(bytesFromBase64(object.proto_bytes))
        : undefined,
    };
  },

  toJSON(message: TypedValue): unknown {
    const obj: any = {};
    if (message.stringVal !== undefined) {
      obj.stringVal = message.stringVal;
    }
    if (message.intVal !== undefined) {
      obj.intVal = message.intVal;
    }
    if (message.uintVal !== undefined) {
      obj.uintVal = message.uintVal;
    }
    if (message.boolVal !== undefined) {
      obj.boolVal = message.boolVal;
    }
    if (message.bytesVal !== undefined) {
      obj.bytesVal = base64FromBytes(message.bytesVal);
    }
    if (message.floatVal !== undefined) {
      obj.floatVal = message.floatVal;
    }
    if (message.doubleVal !== undefined) {
      obj.doubleVal = message.doubleVal;
    }
    if (message.decimalVal !== undefined) {
      obj.decimalVal = Decimal64.toJSON(message.decimalVal);
    }
    if (message.leaflistVal !== undefined) {
      obj.leaflistVal = ScalarArray.toJSON(message.leaflistVal);
    }
    if (message.anyVal !== undefined) {
      obj.anyVal = Any.toJSON(message.anyVal);
    }
    if (message.jsonVal !== undefined) {
      obj.jsonVal = base64FromBytes(message.jsonVal);
    }
    if (message.jsonIetfVal !== undefined) {
      obj.jsonIetfVal = base64FromBytes(message.jsonIetfVal);
    }
    if (message.asciiVal !== undefined) {
      obj.asciiVal = message.asciiVal;
    }
    if (message.protoBytes !== undefined) {
      obj.protoBytes = base64FromBytes(message.protoBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TypedValue>, I>>(base?: I): TypedValue {
    return TypedValue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TypedValue>, I>>(object: I): TypedValue {
    const message = createBaseTypedValue();
    message.stringVal = object.stringVal ?? undefined;
    message.intVal = object.intVal ?? undefined;
    message.uintVal = object.uintVal ?? undefined;
    message.boolVal = object.boolVal ?? undefined;
    message.bytesVal = object.bytesVal ?? undefined;
    message.floatVal = object.floatVal ?? undefined;
    message.doubleVal = object.doubleVal ?? undefined;
    message.decimalVal = (object.decimalVal !== undefined && object.decimalVal !== null)
      ? Decimal64.fromPartial(object.decimalVal)
      : undefined;
    message.leaflistVal = (object.leaflistVal !== undefined && object.leaflistVal !== null)
      ? ScalarArray.fromPartial(object.leaflistVal)
      : undefined;
    message.anyVal = (object.anyVal !== undefined && object.anyVal !== null)
      ? Any.fromPartial(object.anyVal)
      : undefined;
    message.jsonVal = object.jsonVal ?? undefined;
    message.jsonIetfVal = object.jsonIetfVal ?? undefined;
    message.asciiVal = object.asciiVal ?? undefined;
    message.protoBytes = object.protoBytes ?? undefined;
    return message;
  },
};

function createBasePath(): Path {
  return { element: [], origin: "", elem: [], target: "" };
}

export const Path: MessageFns<Path> = {
  encode(message: Path, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.element) {
      writer.uint32(10).string(v!);
    }
    if (message.origin !== "") {
      writer.uint32(18).string(message.origin);
    }
    for (const v of message.elem) {
      PathElem.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.target !== "") {
      writer.uint32(34).string(message.target);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Path {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePath();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.element.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.origin = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.elem.push(PathElem.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.target = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Path {
    return {
      element: globalThis.Array.isArray(object?.element) ? object.element.map((e: any) => globalThis.String(e)) : [],
      origin: isSet(object.origin) ? globalThis.String(object.origin) : "",
      elem: globalThis.Array.isArray(object?.elem) ? object.elem.map((e: any) => PathElem.fromJSON(e)) : [],
      target: isSet(object.target) ? globalThis.String(object.target) : "",
    };
  },

  toJSON(message: Path): unknown {
    const obj: any = {};
    if (message.element?.length) {
      obj.element = message.element;
    }
    if (message.origin !== "") {
      obj.origin = message.origin;
    }
    if (message.elem?.length) {
      obj.elem = message.elem.map((e) => PathElem.toJSON(e));
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Path>, I>>(base?: I): Path {
    return Path.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Path>, I>>(object: I): Path {
    const message = createBasePath();
    message.element = object.element?.map((e) => e) || [];
    message.origin = object.origin ?? "";
    message.elem = object.elem?.map((e) => PathElem.fromPartial(e)) || [];
    message.target = object.target ?? "";
    return message;
  },
};

function createBasePathElem(): PathElem {
  return { name: "", key: {} };
}

export const PathElem: MessageFns<PathElem> = {
  encode(message: PathElem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    globalThis.Object.entries(message.key).forEach(([key, value]: [string, string]) => {
      PathElem_KeyEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PathElem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePathElem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          const entry2 = PathElem_KeyEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.key[entry2.key] = entry2.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PathElem {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      key: isObject(object.key)
        ? (globalThis.Object.entries(object.key) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PathElem): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.key) {
      const entries = globalThis.Object.entries(message.key) as [string, string][];
      if (entries.length > 0) {
        obj.key = {};
        entries.forEach(([k, v]) => {
          obj.key[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PathElem>, I>>(base?: I): PathElem {
    return PathElem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PathElem>, I>>(object: I): PathElem {
    const message = createBasePathElem();
    message.name = object.name ?? "";
    message.key = (globalThis.Object.entries(object.key ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePathElem_KeyEntry(): PathElem_KeyEntry {
  return { key: "", value: "" };
}

export const PathElem_KeyEntry: MessageFns<PathElem_KeyEntry> = {
  encode(message: PathElem_KeyEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PathElem_KeyEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePathElem_KeyEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PathElem_KeyEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PathElem_KeyEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PathElem_KeyEntry>, I>>(base?: I): PathElem_KeyEntry {
    return PathElem_KeyEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PathElem_KeyEntry>, I>>(object: I): PathElem_KeyEntry {
    const message = createBasePathElem_KeyEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseValue(): Value {
  return { value: Buffer.alloc(0), type: 0 };
}

export const Value: MessageFns<Value> = {
  encode(message: Value, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.value.length !== 0) {
      writer.uint32(10).bytes(message.value);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Value {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseValue();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Value {
    return {
      value: isSet(object.value) ? Buffer.from(bytesFromBase64(object.value)) : Buffer.alloc(0),
      type: isSet(object.type) ? encodingFromJSON(object.type) : 0,
    };
  },

  toJSON(message: Value): unknown {
    const obj: any = {};
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    if (message.type !== 0) {
      obj.type = encodingToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Value>, I>>(base?: I): Value {
    return Value.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Value>, I>>(object: I): Value {
    const message = createBaseValue();
    message.value = object.value ?? Buffer.alloc(0);
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseError(): Error {
  return { code: 0, message: "", data: undefined };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== 0) {
      writer.uint32(8).uint32(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.data !== undefined) {
      Any.encode(message.data, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.code = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.data = Any.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Error {
    return {
      code: isSet(object.code) ? globalThis.Number(object.code) : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      data: isSet(object.data) ? Any.fromJSON(object.data) : undefined,
    };
  },

  toJSON(message: Error): unknown {
    const obj: any = {};
    if (message.code !== 0) {
      obj.code = Math.round(message.code);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.data !== undefined) {
      obj.data = Any.toJSON(message.data);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Error>, I>>(base?: I): Error {
    return Error.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Error>, I>>(object: I): Error {
    const message = createBaseError();
    message.code = object.code ?? 0;
    message.message = object.message ?? "";
    message.data = (object.data !== undefined && object.data !== null) ? Any.fromPartial(object.data) : undefined;
    return message;
  },
};

function createBaseDecimal64(): Decimal64 {
  return { digits: "0", precision: 0 };
}

export const Decimal64: MessageFns<Decimal64> = {
  encode(message: Decimal64, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.digits !== "0") {
      writer.uint32(8).int64(message.digits);
    }
    if (message.precision !== 0) {
      writer.uint32(16).uint32(message.precision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Decimal64 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDecimal64();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.digits = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.precision = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Decimal64 {
    return {
      digits: isSet(object.digits) ? globalThis.String(object.digits) : "0",
      precision: isSet(object.precision) ? globalThis.Number(object.precision) : 0,
    };
  },

  toJSON(message: Decimal64): unknown {
    const obj: any = {};
    if (message.digits !== "0") {
      obj.digits = message.digits;
    }
    if (message.precision !== 0) {
      obj.precision = Math.round(message.precision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Decimal64>, I>>(base?: I): Decimal64 {
    return Decimal64.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Decimal64>, I>>(object: I): Decimal64 {
    const message = createBaseDecimal64();
    message.digits = object.digits ?? "0";
    message.precision = object.precision ?? 0;
    return message;
  },
};

function createBaseScalarArray(): ScalarArray {
  return { element: [] };
}

export const ScalarArray: MessageFns<ScalarArray> = {
  encode(message: ScalarArray, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.element) {
      TypedValue.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScalarArray {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScalarArray();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.element.push(TypedValue.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScalarArray {
    return {
      element: globalThis.Array.isArray(object?.element) ? object.element.map((e: any) => TypedValue.fromJSON(e)) : [],
    };
  },

  toJSON(message: ScalarArray): unknown {
    const obj: any = {};
    if (message.element?.length) {
      obj.element = message.element.map((e) => TypedValue.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ScalarArray>, I>>(base?: I): ScalarArray {
    return ScalarArray.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ScalarArray>, I>>(object: I): ScalarArray {
    const message = createBaseScalarArray();
    message.element = object.element?.map((e) => TypedValue.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSubscribeRequest(): SubscribeRequest {
  return { subscribe: undefined, poll: undefined, extension: [] };
}

export const SubscribeRequest: MessageFns<SubscribeRequest> = {
  encode(message: SubscribeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subscribe !== undefined) {
      SubscriptionList.encode(message.subscribe, writer.uint32(10).fork()).join();
    }
    if (message.poll !== undefined) {
      Poll.encode(message.poll, writer.uint32(26).fork()).join();
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscribe = SubscriptionList.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.poll = Poll.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeRequest {
    return {
      subscribe: isSet(object.subscribe) ? SubscriptionList.fromJSON(object.subscribe) : undefined,
      poll: isSet(object.poll) ? Poll.fromJSON(object.poll) : undefined,
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SubscribeRequest): unknown {
    const obj: any = {};
    if (message.subscribe !== undefined) {
      obj.subscribe = SubscriptionList.toJSON(message.subscribe);
    }
    if (message.poll !== undefined) {
      obj.poll = Poll.toJSON(message.poll);
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeRequest>, I>>(base?: I): SubscribeRequest {
    return SubscribeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeRequest>, I>>(object: I): SubscribeRequest {
    const message = createBaseSubscribeRequest();
    message.subscribe = (object.subscribe !== undefined && object.subscribe !== null)
      ? SubscriptionList.fromPartial(object.subscribe)
      : undefined;
    message.poll = (object.poll !== undefined && object.poll !== null) ? Poll.fromPartial(object.poll) : undefined;
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBasePoll(): Poll {
  return {};
}

export const Poll: MessageFns<Poll> = {
  encode(_: Poll, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Poll {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePoll();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Poll {
    return {};
  },

  toJSON(_: Poll): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<Poll>, I>>(base?: I): Poll {
    return Poll.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Poll>, I>>(_: I): Poll {
    const message = createBasePoll();
    return message;
  },
};

function createBaseSubscribeResponse(): SubscribeResponse {
  return { update: undefined, syncResponse: undefined, error: undefined, extension: [] };
}

export const SubscribeResponse: MessageFns<SubscribeResponse> = {
  encode(message: SubscribeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.update !== undefined) {
      Notification.encode(message.update, writer.uint32(10).fork()).join();
    }
    if (message.syncResponse !== undefined) {
      writer.uint32(24).bool(message.syncResponse);
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(34).fork()).join();
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscribeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscribeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.update = Notification.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.syncResponse = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscribeResponse {
    return {
      update: isSet(object.update) ? Notification.fromJSON(object.update) : undefined,
      syncResponse: isSet(object.syncResponse)
        ? globalThis.Boolean(object.syncResponse)
        : isSet(object.sync_response)
        ? globalThis.Boolean(object.sync_response)
        : undefined,
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SubscribeResponse): unknown {
    const obj: any = {};
    if (message.update !== undefined) {
      obj.update = Notification.toJSON(message.update);
    }
    if (message.syncResponse !== undefined) {
      obj.syncResponse = message.syncResponse;
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscribeResponse>, I>>(base?: I): SubscribeResponse {
    return SubscribeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscribeResponse>, I>>(object: I): SubscribeResponse {
    const message = createBaseSubscribeResponse();
    message.update = (object.update !== undefined && object.update !== null)
      ? Notification.fromPartial(object.update)
      : undefined;
    message.syncResponse = object.syncResponse ?? undefined;
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSubscriptionList(): SubscriptionList {
  return {
    prefix: undefined,
    subscription: [],
    qos: undefined,
    mode: 0,
    allowAggregation: false,
    useModels: [],
    encoding: 0,
    updatesOnly: false,
  };
}

export const SubscriptionList: MessageFns<SubscriptionList> = {
  encode(message: SubscriptionList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== undefined) {
      Path.encode(message.prefix, writer.uint32(10).fork()).join();
    }
    for (const v of message.subscription) {
      Subscription.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.qos !== undefined) {
      QOSMarking.encode(message.qos, writer.uint32(34).fork()).join();
    }
    if (message.mode !== 0) {
      writer.uint32(40).int32(message.mode);
    }
    if (message.allowAggregation !== false) {
      writer.uint32(48).bool(message.allowAggregation);
    }
    for (const v of message.useModels) {
      ModelData.encode(v!, writer.uint32(58).fork()).join();
    }
    if (message.encoding !== 0) {
      writer.uint32(64).int32(message.encoding);
    }
    if (message.updatesOnly !== false) {
      writer.uint32(72).bool(message.updatesOnly);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SubscriptionList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscriptionList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.subscription.push(Subscription.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.qos = QOSMarking.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.allowAggregation = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.useModels.push(ModelData.decode(reader, reader.uint32()));
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.encoding = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.updatesOnly = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SubscriptionList {
    return {
      prefix: isSet(object.prefix) ? Path.fromJSON(object.prefix) : undefined,
      subscription: globalThis.Array.isArray(object?.subscription)
        ? object.subscription.map((e: any) => Subscription.fromJSON(e))
        : [],
      qos: isSet(object.qos) ? QOSMarking.fromJSON(object.qos) : undefined,
      mode: isSet(object.mode) ? subscriptionList_ModeFromJSON(object.mode) : 0,
      allowAggregation: isSet(object.allowAggregation)
        ? globalThis.Boolean(object.allowAggregation)
        : isSet(object.allow_aggregation)
        ? globalThis.Boolean(object.allow_aggregation)
        : false,
      useModels: globalThis.Array.isArray(object?.useModels)
        ? object.useModels.map((e: any) => ModelData.fromJSON(e))
        : globalThis.Array.isArray(object?.use_models)
        ? object.use_models.map((e: any) => ModelData.fromJSON(e))
        : [],
      encoding: isSet(object.encoding) ? encodingFromJSON(object.encoding) : 0,
      updatesOnly: isSet(object.updatesOnly)
        ? globalThis.Boolean(object.updatesOnly)
        : isSet(object.updates_only)
        ? globalThis.Boolean(object.updates_only)
        : false,
    };
  },

  toJSON(message: SubscriptionList): unknown {
    const obj: any = {};
    if (message.prefix !== undefined) {
      obj.prefix = Path.toJSON(message.prefix);
    }
    if (message.subscription?.length) {
      obj.subscription = message.subscription.map((e) => Subscription.toJSON(e));
    }
    if (message.qos !== undefined) {
      obj.qos = QOSMarking.toJSON(message.qos);
    }
    if (message.mode !== 0) {
      obj.mode = subscriptionList_ModeToJSON(message.mode);
    }
    if (message.allowAggregation !== false) {
      obj.allowAggregation = message.allowAggregation;
    }
    if (message.useModels?.length) {
      obj.useModels = message.useModels.map((e) => ModelData.toJSON(e));
    }
    if (message.encoding !== 0) {
      obj.encoding = encodingToJSON(message.encoding);
    }
    if (message.updatesOnly !== false) {
      obj.updatesOnly = message.updatesOnly;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SubscriptionList>, I>>(base?: I): SubscriptionList {
    return SubscriptionList.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SubscriptionList>, I>>(object: I): SubscriptionList {
    const message = createBaseSubscriptionList();
    message.prefix = (object.prefix !== undefined && object.prefix !== null)
      ? Path.fromPartial(object.prefix)
      : undefined;
    message.subscription = object.subscription?.map((e) => Subscription.fromPartial(e)) || [];
    message.qos = (object.qos !== undefined && object.qos !== null) ? QOSMarking.fromPartial(object.qos) : undefined;
    message.mode = object.mode ?? 0;
    message.allowAggregation = object.allowAggregation ?? false;
    message.useModels = object.useModels?.map((e) => ModelData.fromPartial(e)) || [];
    message.encoding = object.encoding ?? 0;
    message.updatesOnly = object.updatesOnly ?? false;
    return message;
  },
};

function createBaseSubscription(): Subscription {
  return { path: undefined, mode: 0, sampleInterval: "0", suppressRedundant: false, heartbeatInterval: "0" };
}

export const Subscription: MessageFns<Subscription> = {
  encode(message: Subscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      Path.encode(message.path, writer.uint32(10).fork()).join();
    }
    if (message.mode !== 0) {
      writer.uint32(16).int32(message.mode);
    }
    if (message.sampleInterval !== "0") {
      writer.uint32(24).uint64(message.sampleInterval);
    }
    if (message.suppressRedundant !== false) {
      writer.uint32(32).bool(message.suppressRedundant);
    }
    if (message.heartbeatInterval !== "0") {
      writer.uint32(40).uint64(message.heartbeatInterval);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Subscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.mode = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sampleInterval = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.suppressRedundant = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.heartbeatInterval = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Subscription {
    return {
      path: isSet(object.path) ? Path.fromJSON(object.path) : undefined,
      mode: isSet(object.mode) ? subscriptionModeFromJSON(object.mode) : 0,
      sampleInterval: isSet(object.sampleInterval)
        ? globalThis.String(object.sampleInterval)
        : isSet(object.sample_interval)
        ? globalThis.String(object.sample_interval)
        : "0",
      suppressRedundant: isSet(object.suppressRedundant)
        ? globalThis.Boolean(object.suppressRedundant)
        : isSet(object.suppress_redundant)
        ? globalThis.Boolean(object.suppress_redundant)
        : false,
      heartbeatInterval: isSet(object.heartbeatInterval)
        ? globalThis.String(object.heartbeatInterval)
        : isSet(object.heartbeat_interval)
        ? globalThis.String(object.heartbeat_interval)
        : "0",
    };
  },

  toJSON(message: Subscription): unknown {
    const obj: any = {};
    if (message.path !== undefined) {
      obj.path = Path.toJSON(message.path);
    }
    if (message.mode !== 0) {
      obj.mode = subscriptionModeToJSON(message.mode);
    }
    if (message.sampleInterval !== "0") {
      obj.sampleInterval = message.sampleInterval;
    }
    if (message.suppressRedundant !== false) {
      obj.suppressRedundant = message.suppressRedundant;
    }
    if (message.heartbeatInterval !== "0") {
      obj.heartbeatInterval = message.heartbeatInterval;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Subscription>, I>>(base?: I): Subscription {
    return Subscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Subscription>, I>>(object: I): Subscription {
    const message = createBaseSubscription();
    message.path = (object.path !== undefined && object.path !== null) ? Path.fromPartial(object.path) : undefined;
    message.mode = object.mode ?? 0;
    message.sampleInterval = object.sampleInterval ?? "0";
    message.suppressRedundant = object.suppressRedundant ?? false;
    message.heartbeatInterval = object.heartbeatInterval ?? "0";
    return message;
  },
};

function createBaseQOSMarking(): QOSMarking {
  return { marking: 0 };
}

export const QOSMarking: MessageFns<QOSMarking> = {
  encode(message: QOSMarking, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.marking !== 0) {
      writer.uint32(8).uint32(message.marking);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QOSMarking {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQOSMarking();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.marking = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QOSMarking {
    return { marking: isSet(object.marking) ? globalThis.Number(object.marking) : 0 };
  },

  toJSON(message: QOSMarking): unknown {
    const obj: any = {};
    if (message.marking !== 0) {
      obj.marking = Math.round(message.marking);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QOSMarking>, I>>(base?: I): QOSMarking {
    return QOSMarking.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QOSMarking>, I>>(object: I): QOSMarking {
    const message = createBaseQOSMarking();
    message.marking = object.marking ?? 0;
    return message;
  },
};

function createBaseSetRequest(): SetRequest {
  return { prefix: undefined, delete: [], replace: [], update: [], unionReplace: [], extension: [] };
}

export const SetRequest: MessageFns<SetRequest> = {
  encode(message: SetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== undefined) {
      Path.encode(message.prefix, writer.uint32(10).fork()).join();
    }
    for (const v of message.delete) {
      Path.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.replace) {
      Update.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.update) {
      Update.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.unionReplace) {
      Update.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.delete.push(Path.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.replace.push(Update.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.update.push(Update.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.unionReplace.push(Update.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetRequest {
    return {
      prefix: isSet(object.prefix) ? Path.fromJSON(object.prefix) : undefined,
      delete: globalThis.Array.isArray(object?.delete) ? object.delete.map((e: any) => Path.fromJSON(e)) : [],
      replace: globalThis.Array.isArray(object?.replace) ? object.replace.map((e: any) => Update.fromJSON(e)) : [],
      update: globalThis.Array.isArray(object?.update) ? object.update.map((e: any) => Update.fromJSON(e)) : [],
      unionReplace: globalThis.Array.isArray(object?.unionReplace)
        ? object.unionReplace.map((e: any) => Update.fromJSON(e))
        : globalThis.Array.isArray(object?.union_replace)
        ? object.union_replace.map((e: any) => Update.fromJSON(e))
        : [],
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SetRequest): unknown {
    const obj: any = {};
    if (message.prefix !== undefined) {
      obj.prefix = Path.toJSON(message.prefix);
    }
    if (message.delete?.length) {
      obj.delete = message.delete.map((e) => Path.toJSON(e));
    }
    if (message.replace?.length) {
      obj.replace = message.replace.map((e) => Update.toJSON(e));
    }
    if (message.update?.length) {
      obj.update = message.update.map((e) => Update.toJSON(e));
    }
    if (message.unionReplace?.length) {
      obj.unionReplace = message.unionReplace.map((e) => Update.toJSON(e));
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetRequest>, I>>(base?: I): SetRequest {
    return SetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetRequest>, I>>(object: I): SetRequest {
    const message = createBaseSetRequest();
    message.prefix = (object.prefix !== undefined && object.prefix !== null)
      ? Path.fromPartial(object.prefix)
      : undefined;
    message.delete = object.delete?.map((e) => Path.fromPartial(e)) || [];
    message.replace = object.replace?.map((e) => Update.fromPartial(e)) || [];
    message.update = object.update?.map((e) => Update.fromPartial(e)) || [];
    message.unionReplace = object.unionReplace?.map((e) => Update.fromPartial(e)) || [];
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSetResponse(): SetResponse {
  return { prefix: undefined, response: [], message: undefined, timestamp: "0", extension: [] };
}

export const SetResponse: MessageFns<SetResponse> = {
  encode(message: SetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== undefined) {
      Path.encode(message.prefix, writer.uint32(10).fork()).join();
    }
    for (const v of message.response) {
      UpdateResult.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.message !== undefined) {
      Error.encode(message.message, writer.uint32(26).fork()).join();
    }
    if (message.timestamp !== "0") {
      writer.uint32(32).int64(message.timestamp);
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.response.push(UpdateResult.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = Error.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.timestamp = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetResponse {
    return {
      prefix: isSet(object.prefix) ? Path.fromJSON(object.prefix) : undefined,
      response: globalThis.Array.isArray(object?.response)
        ? object.response.map((e: any) => UpdateResult.fromJSON(e))
        : [],
      message: isSet(object.message) ? Error.fromJSON(object.message) : undefined,
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "0",
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SetResponse): unknown {
    const obj: any = {};
    if (message.prefix !== undefined) {
      obj.prefix = Path.toJSON(message.prefix);
    }
    if (message.response?.length) {
      obj.response = message.response.map((e) => UpdateResult.toJSON(e));
    }
    if (message.message !== undefined) {
      obj.message = Error.toJSON(message.message);
    }
    if (message.timestamp !== "0") {
      obj.timestamp = message.timestamp;
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SetResponse>, I>>(base?: I): SetResponse {
    return SetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SetResponse>, I>>(object: I): SetResponse {
    const message = createBaseSetResponse();
    message.prefix = (object.prefix !== undefined && object.prefix !== null)
      ? Path.fromPartial(object.prefix)
      : undefined;
    message.response = object.response?.map((e) => UpdateResult.fromPartial(e)) || [];
    message.message = (object.message !== undefined && object.message !== null)
      ? Error.fromPartial(object.message)
      : undefined;
    message.timestamp = object.timestamp ?? "0";
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUpdateResult(): UpdateResult {
  return { timestamp: "0", path: undefined, message: undefined, op: 0 };
}

export const UpdateResult: MessageFns<UpdateResult> = {
  encode(message: UpdateResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== "0") {
      writer.uint32(8).int64(message.timestamp);
    }
    if (message.path !== undefined) {
      Path.encode(message.path, writer.uint32(18).fork()).join();
    }
    if (message.message !== undefined) {
      Error.encode(message.message, writer.uint32(26).fork()).join();
    }
    if (message.op !== 0) {
      writer.uint32(32).int32(message.op);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.timestamp = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path = Path.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.message = Error.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.op = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateResult {
    return {
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "0",
      path: isSet(object.path) ? Path.fromJSON(object.path) : undefined,
      message: isSet(object.message) ? Error.fromJSON(object.message) : undefined,
      op: isSet(object.op) ? updateResult_OperationFromJSON(object.op) : 0,
    };
  },

  toJSON(message: UpdateResult): unknown {
    const obj: any = {};
    if (message.timestamp !== "0") {
      obj.timestamp = message.timestamp;
    }
    if (message.path !== undefined) {
      obj.path = Path.toJSON(message.path);
    }
    if (message.message !== undefined) {
      obj.message = Error.toJSON(message.message);
    }
    if (message.op !== 0) {
      obj.op = updateResult_OperationToJSON(message.op);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateResult>, I>>(base?: I): UpdateResult {
    return UpdateResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateResult>, I>>(object: I): UpdateResult {
    const message = createBaseUpdateResult();
    message.timestamp = object.timestamp ?? "0";
    message.path = (object.path !== undefined && object.path !== null) ? Path.fromPartial(object.path) : undefined;
    message.message = (object.message !== undefined && object.message !== null)
      ? Error.fromPartial(object.message)
      : undefined;
    message.op = object.op ?? 0;
    return message;
  },
};

function createBaseGetRequest(): GetRequest {
  return { prefix: undefined, path: [], type: 0, encoding: 0, useModels: [], extension: [] };
}

export const GetRequest: MessageFns<GetRequest> = {
  encode(message: GetRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.prefix !== undefined) {
      Path.encode(message.prefix, writer.uint32(10).fork()).join();
    }
    for (const v of message.path) {
      Path.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.encoding !== 0) {
      writer.uint32(40).int32(message.encoding);
    }
    for (const v of message.useModels) {
      ModelData.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.prefix = Path.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.path.push(Path.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.encoding = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.useModels.push(ModelData.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetRequest {
    return {
      prefix: isSet(object.prefix) ? Path.fromJSON(object.prefix) : undefined,
      path: globalThis.Array.isArray(object?.path) ? object.path.map((e: any) => Path.fromJSON(e)) : [],
      type: isSet(object.type) ? getRequest_DataTypeFromJSON(object.type) : 0,
      encoding: isSet(object.encoding) ? encodingFromJSON(object.encoding) : 0,
      useModels: globalThis.Array.isArray(object?.useModels)
        ? object.useModels.map((e: any) => ModelData.fromJSON(e))
        : globalThis.Array.isArray(object?.use_models)
        ? object.use_models.map((e: any) => ModelData.fromJSON(e))
        : [],
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetRequest): unknown {
    const obj: any = {};
    if (message.prefix !== undefined) {
      obj.prefix = Path.toJSON(message.prefix);
    }
    if (message.path?.length) {
      obj.path = message.path.map((e) => Path.toJSON(e));
    }
    if (message.type !== 0) {
      obj.type = getRequest_DataTypeToJSON(message.type);
    }
    if (message.encoding !== 0) {
      obj.encoding = encodingToJSON(message.encoding);
    }
    if (message.useModels?.length) {
      obj.useModels = message.useModels.map((e) => ModelData.toJSON(e));
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetRequest>, I>>(base?: I): GetRequest {
    return GetRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetRequest>, I>>(object: I): GetRequest {
    const message = createBaseGetRequest();
    message.prefix = (object.prefix !== undefined && object.prefix !== null)
      ? Path.fromPartial(object.prefix)
      : undefined;
    message.path = object.path?.map((e) => Path.fromPartial(e)) || [];
    message.type = object.type ?? 0;
    message.encoding = object.encoding ?? 0;
    message.useModels = object.useModels?.map((e) => ModelData.fromPartial(e)) || [];
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetResponse(): GetResponse {
  return { notification: [], error: undefined, extension: [] };
}

export const GetResponse: MessageFns<GetResponse> = {
  encode(message: GetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.notification) {
      Notification.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(18).fork()).join();
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.notification.push(Notification.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = Error.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetResponse {
    return {
      notification: globalThis.Array.isArray(object?.notification)
        ? object.notification.map((e: any) => Notification.fromJSON(e))
        : [],
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetResponse): unknown {
    const obj: any = {};
    if (message.notification?.length) {
      obj.notification = message.notification.map((e) => Notification.toJSON(e));
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetResponse>, I>>(base?: I): GetResponse {
    return GetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetResponse>, I>>(object: I): GetResponse {
    const message = createBaseGetResponse();
    message.notification = object.notification?.map((e) => Notification.fromPartial(e)) || [];
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityRequest(): CapabilityRequest {
  return { extension: [] };
}

export const CapabilityRequest: MessageFns<CapabilityRequest> = {
  encode(message: CapabilityRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityRequest {
    return {
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CapabilityRequest): unknown {
    const obj: any = {};
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityRequest>, I>>(base?: I): CapabilityRequest {
    return CapabilityRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityRequest>, I>>(object: I): CapabilityRequest {
    const message = createBaseCapabilityRequest();
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCapabilityResponse(): CapabilityResponse {
  return { supportedModels: [], supportedEncodings: [], gNMIVersion: "", extension: [] };
}

export const CapabilityResponse: MessageFns<CapabilityResponse> = {
  encode(message: CapabilityResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.supportedModels) {
      ModelData.encode(v!, writer.uint32(10).fork()).join();
    }
    writer.uint32(18).fork();
    for (const v of message.supportedEncodings) {
      writer.int32(v);
    }
    writer.join();
    if (message.gNMIVersion !== "") {
      writer.uint32(26).string(message.gNMIVersion);
    }
    for (const v of message.extension) {
      Extension.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CapabilityResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilityResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.supportedModels.push(ModelData.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.supportedEncodings.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedEncodings.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.gNMIVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.extension.push(Extension.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CapabilityResponse {
    return {
      supportedModels: globalThis.Array.isArray(object?.supportedModels)
        ? object.supportedModels.map((e: any) => ModelData.fromJSON(e))
        : globalThis.Array.isArray(object?.supported_models)
        ? object.supported_models.map((e: any) => ModelData.fromJSON(e))
        : [],
      supportedEncodings: globalThis.Array.isArray(object?.supportedEncodings)
        ? object.supportedEncodings.map((e: any) => encodingFromJSON(e))
        : globalThis.Array.isArray(object?.supported_encodings)
        ? object.supported_encodings.map((e: any) => encodingFromJSON(e))
        : [],
      gNMIVersion: isSet(object.gNMIVersion)
        ? globalThis.String(object.gNMIVersion)
        : isSet(object.gNMI_version)
        ? globalThis.String(object.gNMI_version)
        : "",
      extension: globalThis.Array.isArray(object?.extension)
        ? object.extension.map((e: any) => Extension.fromJSON(e))
        : [],
    };
  },

  toJSON(message: CapabilityResponse): unknown {
    const obj: any = {};
    if (message.supportedModels?.length) {
      obj.supportedModels = message.supportedModels.map((e) => ModelData.toJSON(e));
    }
    if (message.supportedEncodings?.length) {
      obj.supportedEncodings = message.supportedEncodings.map((e) => encodingToJSON(e));
    }
    if (message.gNMIVersion !== "") {
      obj.gNMIVersion = message.gNMIVersion;
    }
    if (message.extension?.length) {
      obj.extension = message.extension.map((e) => Extension.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CapabilityResponse>, I>>(base?: I): CapabilityResponse {
    return CapabilityResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CapabilityResponse>, I>>(object: I): CapabilityResponse {
    const message = createBaseCapabilityResponse();
    message.supportedModels = object.supportedModels?.map((e) => ModelData.fromPartial(e)) || [];
    message.supportedEncodings = object.supportedEncodings?.map((e) => e) || [];
    message.gNMIVersion = object.gNMIVersion ?? "";
    message.extension = object.extension?.map((e) => Extension.fromPartial(e)) || [];
    return message;
  },
};

function createBaseModelData(): ModelData {
  return { name: "", organization: "", version: "" };
}

export const ModelData: MessageFns<ModelData> = {
  encode(message: ModelData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.organization !== "") {
      writer.uint32(18).string(message.organization);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModelData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModelData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.organization = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModelData {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      organization: isSet(object.organization) ? globalThis.String(object.organization) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: ModelData): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.organization !== "") {
      obj.organization = message.organization;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModelData>, I>>(base?: I): ModelData {
    return ModelData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModelData>, I>>(object: I): ModelData {
    const message = createBaseModelData();
    message.name = object.name ?? "";
    message.organization = object.organization ?? "";
    message.version = object.version ?? "";
    return message;
  },
};

export type gNMIService = typeof gNMIService;
export const gNMIService = {
  /**
   * Capabilities allows the client to retrieve the set of capabilities that
   * is supported by the target. This allows the target to validate the
   * service version that is implemented and retrieve the set of models that
   * the target supports. The models can then be specified in subsequent RPCs
   * to restrict the set of data that is utilized.
   * Reference: gNMI Specification Section 3.2
   */
  capabilities: {
    path: "/gnmi.gNMI/Capabilities" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: CapabilityRequest): Buffer => Buffer.from(CapabilityRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CapabilityRequest => CapabilityRequest.decode(value),
    responseSerialize: (value: CapabilityResponse): Buffer => Buffer.from(CapabilityResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CapabilityResponse => CapabilityResponse.decode(value),
  },
  /**
   * Retrieve a snapshot of data from the target. A Get RPC requests that the
   * target snapshots a subset of the data tree as specified by the paths
   * included in the message and serializes this to be returned to the
   * client using the specified encoding.
   * Reference: gNMI Specification Section 3.3
   */
  get: {
    path: "/gnmi.gNMI/Get" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: GetRequest): Buffer => Buffer.from(GetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetRequest => GetRequest.decode(value),
    responseSerialize: (value: GetResponse): Buffer => Buffer.from(GetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetResponse => GetResponse.decode(value),
  },
  /**
   * Set allows the client to modify the state of data on the target. The
   * paths to modified along with the new values that the client wishes
   * to set the value to.
   * Reference: gNMI Specification Section 3.4
   */
  set: {
    path: "/gnmi.gNMI/Set" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: SetRequest): Buffer => Buffer.from(SetRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SetRequest => SetRequest.decode(value),
    responseSerialize: (value: SetResponse): Buffer => Buffer.from(SetResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SetResponse => SetResponse.decode(value),
  },
  /**
   * Subscribe allows a client to request the target to send it values
   * of particular paths within the data tree. These values may be streamed
   * at a particular cadence (STREAM), sent one off on a long-lived channel
   * (POLL), or sent as a one-off retrieval (ONCE).
   * Reference: gNMI Specification Section 3.5
   */
  subscribe: {
    path: "/gnmi.gNMI/Subscribe" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: SubscribeRequest): Buffer => Buffer.from(SubscribeRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): SubscribeRequest => SubscribeRequest.decode(value),
    responseSerialize: (value: SubscribeResponse): Buffer => Buffer.from(SubscribeResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): SubscribeResponse => SubscribeResponse.decode(value),
  },
} as const;

export interface gNMIServer extends UntypedServiceImplementation {
  /**
   * Capabilities allows the client to retrieve the set of capabilities that
   * is supported by the target. This allows the target to validate the
   * service version that is implemented and retrieve the set of models that
   * the target supports. The models can then be specified in subsequent RPCs
   * to restrict the set of data that is utilized.
   * Reference: gNMI Specification Section 3.2
   */
  capabilities: handleUnaryCall<CapabilityRequest, CapabilityResponse>;
  /**
   * Retrieve a snapshot of data from the target. A Get RPC requests that the
   * target snapshots a subset of the data tree as specified by the paths
   * included in the message and serializes this to be returned to the
   * client using the specified encoding.
   * Reference: gNMI Specification Section 3.3
   */
  get: handleUnaryCall<GetRequest, GetResponse>;
  /**
   * Set allows the client to modify the state of data on the target. The
   * paths to modified along with the new values that the client wishes
   * to set the value to.
   * Reference: gNMI Specification Section 3.4
   */
  set: handleUnaryCall<SetRequest, SetResponse>;
  /**
   * Subscribe allows a client to request the target to send it values
   * of particular paths within the data tree. These values may be streamed
   * at a particular cadence (STREAM), sent one off on a long-lived channel
   * (POLL), or sent as a one-off retrieval (ONCE).
   * Reference: gNMI Specification Section 3.5
   */
  subscribe: handleBidiStreamingCall<SubscribeRequest, SubscribeResponse>;
}

export interface gNMIClient extends Client {
  /**
   * Capabilities allows the client to retrieve the set of capabilities that
   * is supported by the target. This allows the target to validate the
   * service version that is implemented and retrieve the set of models that
   * the target supports. The models can then be specified in subsequent RPCs
   * to restrict the set of data that is utilized.
   * Reference: gNMI Specification Section 3.2
   */
  capabilities(
    request: CapabilityRequest,
    callback: (error: ServiceError | null, response: CapabilityResponse) => void,
  ): ClientUnaryCall;
  capabilities(
    request: CapabilityRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CapabilityResponse) => void,
  ): ClientUnaryCall;
  capabilities(
    request: CapabilityRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CapabilityResponse) => void,
  ): ClientUnaryCall;
  /**
   * Retrieve a snapshot of data from the target. A Get RPC requests that the
   * target snapshots a subset of the data tree as specified by the paths
   * included in the message and serializes this to be returned to the
   * client using the specified encoding.
   * Reference: gNMI Specification Section 3.3
   */
  get(request: GetRequest, callback: (error: ServiceError | null, response: GetResponse) => void): ClientUnaryCall;
  get(
    request: GetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetResponse) => void,
  ): ClientUnaryCall;
  get(
    request: GetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetResponse) => void,
  ): ClientUnaryCall;
  /**
   * Set allows the client to modify the state of data on the target. The
   * paths to modified along with the new values that the client wishes
   * to set the value to.
   * Reference: gNMI Specification Section 3.4
   */
  set(request: SetRequest, callback: (error: ServiceError | null, response: SetResponse) => void): ClientUnaryCall;
  set(
    request: SetRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SetResponse) => void,
  ): ClientUnaryCall;
  set(
    request: SetRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SetResponse) => void,
  ): ClientUnaryCall;
  /**
   * Subscribe allows a client to request the target to send it values
   * of particular paths within the data tree. These values may be streamed
   * at a particular cadence (STREAM), sent one off on a long-lived channel
   * (POLL), or sent as a one-off retrieval (ONCE).
   * Reference: gNMI Specification Section 3.5
   */
  subscribe(): ClientDuplexStream<SubscribeRequest, SubscribeResponse>;
  subscribe(options: Partial<CallOptions>): ClientDuplexStream<SubscribeRequest, SubscribeResponse>;
  subscribe(
    metadata: Metadata,
    options?: Partial<CallOptions>,
  ): ClientDuplexStream<SubscribeRequest, SubscribeResponse>;
}

export const gNMIClient = makeGenericClientConstructor(gNMIService, "gnmi.gNMI") as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): gNMIClient;
  service: typeof gNMIService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
