// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: github.com/openconfig/gnmi/proto/gnmi_ext/gnmi_ext.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Duration } from "../../../../../google/protobuf/duration";

export const protobufPackage = "gnmi_ext";

/**
 * Copyright 2018 Google Inc. All Rights Reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *    http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/**
 * RegisteredExtension is an enumeration acting as a registry for extensions
 * defined by external sources.
 */
export enum ExtensionID {
  /**
   * EID_UNSET - New extensions are to be defined within this enumeration - their definition
   * MUST link to a reference describing their implementation.
   */
  EID_UNSET = 0,
  /**
   * EID_EXPERIMENTAL - An experimental extension that may be used during prototyping of a new
   * extension.
   */
  EID_EXPERIMENTAL = 999,
  UNRECOGNIZED = -1,
}

export function extensionIDFromJSON(object: any): ExtensionID {
  switch (object) {
    case 0:
    case "EID_UNSET":
      return ExtensionID.EID_UNSET;
    case 999:
    case "EID_EXPERIMENTAL":
      return ExtensionID.EID_EXPERIMENTAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ExtensionID.UNRECOGNIZED;
  }
}

export function extensionIDToJSON(object: ExtensionID): string {
  switch (object) {
    case ExtensionID.EID_UNSET:
      return "EID_UNSET";
    case ExtensionID.EID_EXPERIMENTAL:
      return "EID_EXPERIMENTAL";
    case ExtensionID.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** The Extension message contains a single gNMI extension. */
export interface Extension {
  /** A registered extension. */
  registeredExt?:
    | RegisteredExtension
    | undefined;
  /** Well known extensions. */
  masterArbitration?:
    | MasterArbitration
    | undefined;
  /** History extension. */
  history?:
    | History
    | undefined;
  /** Commit confirmed extension. */
  commit?:
    | Commit
    | undefined;
  /** Depth extension. */
  depth?:
    | Depth
    | undefined;
  /** Config Subscription extension. */
  configSubscription?: ConfigSubscription | undefined;
}

/**
 * The RegisteredExtension message defines an extension which is defined outside
 * of this file.
 */
export interface RegisteredExtension {
  /** The unique ID assigned to this extension. */
  id: ExtensionID;
  /** The binary-marshalled protobuf extension payload. */
  msg: Buffer;
}

/**
 * MasterArbitration is used to select the master among multiple gNMI clients
 * with the same Roles. The client with the largest election_id is honored as
 * the master.
 * The document about gNMI master arbitration can be found at
 * https://github.com/openconfig/reference/blob/master/rpc/gnmi/gnmi-master-arbitration.md
 */
export interface MasterArbitration {
  role?: Role | undefined;
  electionId?: Uint128 | undefined;
}

/** Representation of unsigned 128-bit integer. */
export interface Uint128 {
  high: string;
  low: string;
}

/** There can be one master for each role. The role is identified by its id. */
export interface Role {
  /**
   * More fields can be added if needed, for example, to specify what paths the
   * role can read/write.
   */
  id: string;
}

/**
 * The History extension allows clients to request historical data. Its
 * spec can be found at
 * https://github.com/openconfig/reference/blob/master/rpc/gnmi/gnmi-history.md
 */
export interface History {
  /** Nanoseconds since the epoch */
  snapshotTime?: string | undefined;
  range?: TimeRange | undefined;
}

export interface TimeRange {
  /** Nanoseconds since the epoch */
  start: string;
  /** Nanoseconds since the epoch */
  end: string;
}

/**
 * Commit confirmed extension allows automated revert of the configuration after
 * certain duration if an explicit confirmation is not issued. It allows
 * explicit cancellation of the commit during the rollback window. There cannot
 * be more than one commit active at a given time. The document about gNMI
 * commit confirmed can be found at
 * https://github.com/openconfig/reference/blob/master/rpc/gnmi/gnmi-commit-confirmed.md
 */
export interface Commit {
  /**
   * ID is provided by the client during the commit request. During confirm and
   * cancel actions the provided ID should match the ID provided during commit.
   * If ID is not passed in any actions server shall return error.
   * Required.
   */
  id: string;
  /**
   * commit action creates a new commit. If a commit is on-going, server
   * returns error.
   */
  commit?:
    | CommitRequest
    | undefined;
  /**
   * confirm action will confirm an on-going commit, the ID provided during
   * confirm should match the on-going commit ID.
   */
  confirm?:
    | CommitConfirm
    | undefined;
  /**
   * cancel action will cancel an on-going commit, the ID provided during
   * cancel should match the on-going commit ID.
   */
  cancel?:
    | CommitCancel
    | undefined;
  /**
   * set rollback duration action sets the rollback duration of an on-going commit
   * to a new value.
   * The ID provided with the Commit message should match the on-going commit ID.
   */
  setRollbackDuration?: CommitSetRollbackDuration | undefined;
}

/**
 * CommitRequest is used to create a new confirmed commit. It hold additional
 * parameter requried for commit action.
 */
export interface CommitRequest {
  /** Maximum duration to wait for a confirmaton before reverting the commit. */
  rollbackDuration?: Duration | undefined;
}

/**
 * CommitConfirm is used to confirm an on-going commit. It hold additional
 * parameter requried for confirm action.
 */
export interface CommitConfirm {
}

/**
 * CommitCancel is used to cancel an on-going commit. It hold additional
 * parameter requried for cancel action.
 */
export interface CommitCancel {
}

/**
 * CommitSetRollbackDuration is used to set the existing rollback duration value
 * of an on-going commit to a new desired value.
 */
export interface CommitSetRollbackDuration {
  /** Maximum duration to wait for a confirmaton before reverting the commit. */
  rollbackDuration?: Duration | undefined;
}

/**
 * Depth allows clients to specify the depth of the subtree to be returned in
 * the response. The depth is specified as the number of levels below the
 * specified path.
 * The depth is applied to all paths in the Get or Subscribe request.
 * The document about gNMI depth can be found at
 * https://github.com/openconfig/reference/tree/master/rpc/gnmi/gnmi-depth.md
 */
export interface Depth {
  /**
   * The level of the subtree to be returned in the response.
   * Value of 0 means no depth limit and behaves the same as if the extension
   * was not specified.
   * Value of 1 means only the specified path and its direct children will be
   * returned.
   */
  level: number;
}

/**
 * ConfigSubscription extension allows clients to subscribe to configuration
 * schema nodes only.
 */
export interface ConfigSubscription {
  /** ConfigSubscriptionStart is sent by the client in the SubscribeRequest */
  start?:
    | ConfigSubscriptionStart
    | undefined;
  /** ConfigSubscriptionSyncDone is sent by the server in the SubscribeResponse */
  syncDone?: ConfigSubscriptionSyncDone | undefined;
}

/**
 * ConfigSubscriptionStart is used to indicate to a target that for a given set
 * of paths in the SubscribeRequest, the client wishes to receive updates
 * for the configuration schema nodes only.
 */
export interface ConfigSubscriptionStart {
}

/**
 * ConfigSubscriptionSyncDone is sent by the server in the SubscribeResponse
 * after all the updates for the configuration schema nodes have been sent.
 */
export interface ConfigSubscriptionSyncDone {
  /**
   * ID of a commit confirm operation as assigned by the client
   * see Commit Confirm extension for more details.
   */
  commitConfirmId: string;
  /**
   * ID of a commit as might be assigned by the server
   * when registering a commit operation.
   */
  serverCommitId: string;
  /**
   * If true indicates that the server is done processing the updates related to the
   * commit_confirm_id and/or server_commit_id.
   */
  done: boolean;
}

function createBaseExtension(): Extension {
  return {
    registeredExt: undefined,
    masterArbitration: undefined,
    history: undefined,
    commit: undefined,
    depth: undefined,
    configSubscription: undefined,
  };
}

export const Extension: MessageFns<Extension> = {
  encode(message: Extension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registeredExt !== undefined) {
      RegisteredExtension.encode(message.registeredExt, writer.uint32(10).fork()).join();
    }
    if (message.masterArbitration !== undefined) {
      MasterArbitration.encode(message.masterArbitration, writer.uint32(18).fork()).join();
    }
    if (message.history !== undefined) {
      History.encode(message.history, writer.uint32(26).fork()).join();
    }
    if (message.commit !== undefined) {
      Commit.encode(message.commit, writer.uint32(34).fork()).join();
    }
    if (message.depth !== undefined) {
      Depth.encode(message.depth, writer.uint32(42).fork()).join();
    }
    if (message.configSubscription !== undefined) {
      ConfigSubscription.encode(message.configSubscription, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Extension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExtension();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.registeredExt = RegisteredExtension.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.masterArbitration = MasterArbitration.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.history = History.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.commit = Commit.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.depth = Depth.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.configSubscription = ConfigSubscription.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Extension {
    return {
      registeredExt: isSet(object.registeredExt)
        ? RegisteredExtension.fromJSON(object.registeredExt)
        : isSet(object.registered_ext)
        ? RegisteredExtension.fromJSON(object.registered_ext)
        : undefined,
      masterArbitration: isSet(object.masterArbitration)
        ? MasterArbitration.fromJSON(object.masterArbitration)
        : isSet(object.master_arbitration)
        ? MasterArbitration.fromJSON(object.master_arbitration)
        : undefined,
      history: isSet(object.history) ? History.fromJSON(object.history) : undefined,
      commit: isSet(object.commit) ? Commit.fromJSON(object.commit) : undefined,
      depth: isSet(object.depth) ? Depth.fromJSON(object.depth) : undefined,
      configSubscription: isSet(object.configSubscription)
        ? ConfigSubscription.fromJSON(object.configSubscription)
        : isSet(object.config_subscription)
        ? ConfigSubscription.fromJSON(object.config_subscription)
        : undefined,
    };
  },

  toJSON(message: Extension): unknown {
    const obj: any = {};
    if (message.registeredExt !== undefined) {
      obj.registeredExt = RegisteredExtension.toJSON(message.registeredExt);
    }
    if (message.masterArbitration !== undefined) {
      obj.masterArbitration = MasterArbitration.toJSON(message.masterArbitration);
    }
    if (message.history !== undefined) {
      obj.history = History.toJSON(message.history);
    }
    if (message.commit !== undefined) {
      obj.commit = Commit.toJSON(message.commit);
    }
    if (message.depth !== undefined) {
      obj.depth = Depth.toJSON(message.depth);
    }
    if (message.configSubscription !== undefined) {
      obj.configSubscription = ConfigSubscription.toJSON(message.configSubscription);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Extension>, I>>(base?: I): Extension {
    return Extension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Extension>, I>>(object: I): Extension {
    const message = createBaseExtension();
    message.registeredExt = (object.registeredExt !== undefined && object.registeredExt !== null)
      ? RegisteredExtension.fromPartial(object.registeredExt)
      : undefined;
    message.masterArbitration = (object.masterArbitration !== undefined && object.masterArbitration !== null)
      ? MasterArbitration.fromPartial(object.masterArbitration)
      : undefined;
    message.history = (object.history !== undefined && object.history !== null)
      ? History.fromPartial(object.history)
      : undefined;
    message.commit = (object.commit !== undefined && object.commit !== null)
      ? Commit.fromPartial(object.commit)
      : undefined;
    message.depth = (object.depth !== undefined && object.depth !== null) ? Depth.fromPartial(object.depth) : undefined;
    message.configSubscription = (object.configSubscription !== undefined && object.configSubscription !== null)
      ? ConfigSubscription.fromPartial(object.configSubscription)
      : undefined;
    return message;
  },
};

function createBaseRegisteredExtension(): RegisteredExtension {
  return { id: 0, msg: Buffer.alloc(0) };
}

export const RegisteredExtension: MessageFns<RegisteredExtension> = {
  encode(message: RegisteredExtension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.msg.length !== 0) {
      writer.uint32(18).bytes(message.msg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisteredExtension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisteredExtension();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.msg = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisteredExtension {
    return {
      id: isSet(object.id) ? extensionIDFromJSON(object.id) : 0,
      msg: isSet(object.msg) ? Buffer.from(bytesFromBase64(object.msg)) : Buffer.alloc(0),
    };
  },

  toJSON(message: RegisteredExtension): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = extensionIDToJSON(message.id);
    }
    if (message.msg.length !== 0) {
      obj.msg = base64FromBytes(message.msg);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisteredExtension>, I>>(base?: I): RegisteredExtension {
    return RegisteredExtension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisteredExtension>, I>>(object: I): RegisteredExtension {
    const message = createBaseRegisteredExtension();
    message.id = object.id ?? 0;
    message.msg = object.msg ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseMasterArbitration(): MasterArbitration {
  return { role: undefined, electionId: undefined };
}

export const MasterArbitration: MessageFns<MasterArbitration> = {
  encode(message: MasterArbitration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== undefined) {
      Role.encode(message.role, writer.uint32(10).fork()).join();
    }
    if (message.electionId !== undefined) {
      Uint128.encode(message.electionId, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MasterArbitration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMasterArbitration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.role = Role.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.electionId = Uint128.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MasterArbitration {
    return {
      role: isSet(object.role) ? Role.fromJSON(object.role) : undefined,
      electionId: isSet(object.electionId)
        ? Uint128.fromJSON(object.electionId)
        : isSet(object.election_id)
        ? Uint128.fromJSON(object.election_id)
        : undefined,
    };
  },

  toJSON(message: MasterArbitration): unknown {
    const obj: any = {};
    if (message.role !== undefined) {
      obj.role = Role.toJSON(message.role);
    }
    if (message.electionId !== undefined) {
      obj.electionId = Uint128.toJSON(message.electionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MasterArbitration>, I>>(base?: I): MasterArbitration {
    return MasterArbitration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MasterArbitration>, I>>(object: I): MasterArbitration {
    const message = createBaseMasterArbitration();
    message.role = (object.role !== undefined && object.role !== null) ? Role.fromPartial(object.role) : undefined;
    message.electionId = (object.electionId !== undefined && object.electionId !== null)
      ? Uint128.fromPartial(object.electionId)
      : undefined;
    return message;
  },
};

function createBaseUint128(): Uint128 {
  return { high: "0", low: "0" };
}

export const Uint128: MessageFns<Uint128> = {
  encode(message: Uint128, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.high !== "0") {
      writer.uint32(8).uint64(message.high);
    }
    if (message.low !== "0") {
      writer.uint32(16).uint64(message.low);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Uint128 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUint128();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.high = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.low = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Uint128 {
    return {
      high: isSet(object.high) ? globalThis.String(object.high) : "0",
      low: isSet(object.low) ? globalThis.String(object.low) : "0",
    };
  },

  toJSON(message: Uint128): unknown {
    const obj: any = {};
    if (message.high !== "0") {
      obj.high = message.high;
    }
    if (message.low !== "0") {
      obj.low = message.low;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Uint128>, I>>(base?: I): Uint128 {
    return Uint128.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Uint128>, I>>(object: I): Uint128 {
    const message = createBaseUint128();
    message.high = object.high ?? "0";
    message.low = object.low ?? "0";
    return message;
  },
};

function createBaseRole(): Role {
  return { id: "" };
}

export const Role: MessageFns<Role> = {
  encode(message: Role, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Role {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRole();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Role {
    return { id: isSet(object.id) ? globalThis.String(object.id) : "" };
  },

  toJSON(message: Role): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Role>, I>>(base?: I): Role {
    return Role.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Role>, I>>(object: I): Role {
    const message = createBaseRole();
    message.id = object.id ?? "";
    return message;
  },
};

function createBaseHistory(): History {
  return { snapshotTime: undefined, range: undefined };
}

export const History: MessageFns<History> = {
  encode(message: History, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.snapshotTime !== undefined) {
      writer.uint32(8).int64(message.snapshotTime);
    }
    if (message.range !== undefined) {
      TimeRange.encode(message.range, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): History {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHistory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.snapshotTime = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.range = TimeRange.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): History {
    return {
      snapshotTime: isSet(object.snapshotTime)
        ? globalThis.String(object.snapshotTime)
        : isSet(object.snapshot_time)
        ? globalThis.String(object.snapshot_time)
        : undefined,
      range: isSet(object.range) ? TimeRange.fromJSON(object.range) : undefined,
    };
  },

  toJSON(message: History): unknown {
    const obj: any = {};
    if (message.snapshotTime !== undefined) {
      obj.snapshotTime = message.snapshotTime;
    }
    if (message.range !== undefined) {
      obj.range = TimeRange.toJSON(message.range);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<History>, I>>(base?: I): History {
    return History.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<History>, I>>(object: I): History {
    const message = createBaseHistory();
    message.snapshotTime = object.snapshotTime ?? undefined;
    message.range = (object.range !== undefined && object.range !== null)
      ? TimeRange.fromPartial(object.range)
      : undefined;
    return message;
  },
};

function createBaseTimeRange(): TimeRange {
  return { start: "0", end: "0" };
}

export const TimeRange: MessageFns<TimeRange> = {
  encode(message: TimeRange, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== "0") {
      writer.uint32(8).int64(message.start);
    }
    if (message.end !== "0") {
      writer.uint32(16).int64(message.end);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TimeRange {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTimeRange();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.start = reader.int64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.end = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TimeRange {
    return {
      start: isSet(object.start) ? globalThis.String(object.start) : "0",
      end: isSet(object.end) ? globalThis.String(object.end) : "0",
    };
  },

  toJSON(message: TimeRange): unknown {
    const obj: any = {};
    if (message.start !== "0") {
      obj.start = message.start;
    }
    if (message.end !== "0") {
      obj.end = message.end;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TimeRange>, I>>(base?: I): TimeRange {
    return TimeRange.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TimeRange>, I>>(object: I): TimeRange {
    const message = createBaseTimeRange();
    message.start = object.start ?? "0";
    message.end = object.end ?? "0";
    return message;
  },
};

function createBaseCommit(): Commit {
  return { id: "", commit: undefined, confirm: undefined, cancel: undefined, setRollbackDuration: undefined };
}

export const Commit: MessageFns<Commit> = {
  encode(message: Commit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.commit !== undefined) {
      CommitRequest.encode(message.commit, writer.uint32(18).fork()).join();
    }
    if (message.confirm !== undefined) {
      CommitConfirm.encode(message.confirm, writer.uint32(26).fork()).join();
    }
    if (message.cancel !== undefined) {
      CommitCancel.encode(message.cancel, writer.uint32(34).fork()).join();
    }
    if (message.setRollbackDuration !== undefined) {
      CommitSetRollbackDuration.encode(message.setRollbackDuration, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Commit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommit();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commit = CommitRequest.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirm = CommitConfirm.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.cancel = CommitCancel.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.setRollbackDuration = CommitSetRollbackDuration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Commit {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      commit: isSet(object.commit) ? CommitRequest.fromJSON(object.commit) : undefined,
      confirm: isSet(object.confirm) ? CommitConfirm.fromJSON(object.confirm) : undefined,
      cancel: isSet(object.cancel) ? CommitCancel.fromJSON(object.cancel) : undefined,
      setRollbackDuration: isSet(object.setRollbackDuration)
        ? CommitSetRollbackDuration.fromJSON(object.setRollbackDuration)
        : isSet(object.set_rollback_duration)
        ? CommitSetRollbackDuration.fromJSON(object.set_rollback_duration)
        : undefined,
    };
  },

  toJSON(message: Commit): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.commit !== undefined) {
      obj.commit = CommitRequest.toJSON(message.commit);
    }
    if (message.confirm !== undefined) {
      obj.confirm = CommitConfirm.toJSON(message.confirm);
    }
    if (message.cancel !== undefined) {
      obj.cancel = CommitCancel.toJSON(message.cancel);
    }
    if (message.setRollbackDuration !== undefined) {
      obj.setRollbackDuration = CommitSetRollbackDuration.toJSON(message.setRollbackDuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Commit>, I>>(base?: I): Commit {
    return Commit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Commit>, I>>(object: I): Commit {
    const message = createBaseCommit();
    message.id = object.id ?? "";
    message.commit = (object.commit !== undefined && object.commit !== null)
      ? CommitRequest.fromPartial(object.commit)
      : undefined;
    message.confirm = (object.confirm !== undefined && object.confirm !== null)
      ? CommitConfirm.fromPartial(object.confirm)
      : undefined;
    message.cancel = (object.cancel !== undefined && object.cancel !== null)
      ? CommitCancel.fromPartial(object.cancel)
      : undefined;
    message.setRollbackDuration = (object.setRollbackDuration !== undefined && object.setRollbackDuration !== null)
      ? CommitSetRollbackDuration.fromPartial(object.setRollbackDuration)
      : undefined;
    return message;
  },
};

function createBaseCommitRequest(): CommitRequest {
  return { rollbackDuration: undefined };
}

export const CommitRequest: MessageFns<CommitRequest> = {
  encode(message: CommitRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollbackDuration !== undefined) {
      Duration.encode(message.rollbackDuration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommitRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommitRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollbackDuration = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommitRequest {
    return {
      rollbackDuration: isSet(object.rollbackDuration)
        ? Duration.fromJSON(object.rollbackDuration)
        : isSet(object.rollback_duration)
        ? Duration.fromJSON(object.rollback_duration)
        : undefined,
    };
  },

  toJSON(message: CommitRequest): unknown {
    const obj: any = {};
    if (message.rollbackDuration !== undefined) {
      obj.rollbackDuration = Duration.toJSON(message.rollbackDuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommitRequest>, I>>(base?: I): CommitRequest {
    return CommitRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommitRequest>, I>>(object: I): CommitRequest {
    const message = createBaseCommitRequest();
    message.rollbackDuration = (object.rollbackDuration !== undefined && object.rollbackDuration !== null)
      ? Duration.fromPartial(object.rollbackDuration)
      : undefined;
    return message;
  },
};

function createBaseCommitConfirm(): CommitConfirm {
  return {};
}

export const CommitConfirm: MessageFns<CommitConfirm> = {
  encode(_: CommitConfirm, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommitConfirm {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommitConfirm();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CommitConfirm {
    return {};
  },

  toJSON(_: CommitConfirm): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<CommitConfirm>, I>>(base?: I): CommitConfirm {
    return CommitConfirm.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommitConfirm>, I>>(_: I): CommitConfirm {
    const message = createBaseCommitConfirm();
    return message;
  },
};

function createBaseCommitCancel(): CommitCancel {
  return {};
}

export const CommitCancel: MessageFns<CommitCancel> = {
  encode(_: CommitCancel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommitCancel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommitCancel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): CommitCancel {
    return {};
  },

  toJSON(_: CommitCancel): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<CommitCancel>, I>>(base?: I): CommitCancel {
    return CommitCancel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommitCancel>, I>>(_: I): CommitCancel {
    const message = createBaseCommitCancel();
    return message;
  },
};

function createBaseCommitSetRollbackDuration(): CommitSetRollbackDuration {
  return { rollbackDuration: undefined };
}

export const CommitSetRollbackDuration: MessageFns<CommitSetRollbackDuration> = {
  encode(message: CommitSetRollbackDuration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.rollbackDuration !== undefined) {
      Duration.encode(message.rollbackDuration, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CommitSetRollbackDuration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCommitSetRollbackDuration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.rollbackDuration = Duration.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CommitSetRollbackDuration {
    return {
      rollbackDuration: isSet(object.rollbackDuration)
        ? Duration.fromJSON(object.rollbackDuration)
        : isSet(object.rollback_duration)
        ? Duration.fromJSON(object.rollback_duration)
        : undefined,
    };
  },

  toJSON(message: CommitSetRollbackDuration): unknown {
    const obj: any = {};
    if (message.rollbackDuration !== undefined) {
      obj.rollbackDuration = Duration.toJSON(message.rollbackDuration);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CommitSetRollbackDuration>, I>>(base?: I): CommitSetRollbackDuration {
    return CommitSetRollbackDuration.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CommitSetRollbackDuration>, I>>(object: I): CommitSetRollbackDuration {
    const message = createBaseCommitSetRollbackDuration();
    message.rollbackDuration = (object.rollbackDuration !== undefined && object.rollbackDuration !== null)
      ? Duration.fromPartial(object.rollbackDuration)
      : undefined;
    return message;
  },
};

function createBaseDepth(): Depth {
  return { level: 0 };
}

export const Depth: MessageFns<Depth> = {
  encode(message: Depth, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.level !== 0) {
      writer.uint32(8).uint32(message.level);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Depth {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDepth();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.level = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Depth {
    return { level: isSet(object.level) ? globalThis.Number(object.level) : 0 };
  },

  toJSON(message: Depth): unknown {
    const obj: any = {};
    if (message.level !== 0) {
      obj.level = Math.round(message.level);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Depth>, I>>(base?: I): Depth {
    return Depth.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Depth>, I>>(object: I): Depth {
    const message = createBaseDepth();
    message.level = object.level ?? 0;
    return message;
  },
};

function createBaseConfigSubscription(): ConfigSubscription {
  return { start: undefined, syncDone: undefined };
}

export const ConfigSubscription: MessageFns<ConfigSubscription> = {
  encode(message: ConfigSubscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== undefined) {
      ConfigSubscriptionStart.encode(message.start, writer.uint32(10).fork()).join();
    }
    if (message.syncDone !== undefined) {
      ConfigSubscriptionSyncDone.encode(message.syncDone, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSubscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.start = ConfigSubscriptionStart.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.syncDone = ConfigSubscriptionSyncDone.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSubscription {
    return {
      start: isSet(object.start) ? ConfigSubscriptionStart.fromJSON(object.start) : undefined,
      syncDone: isSet(object.syncDone)
        ? ConfigSubscriptionSyncDone.fromJSON(object.syncDone)
        : isSet(object.sync_done)
        ? ConfigSubscriptionSyncDone.fromJSON(object.sync_done)
        : undefined,
    };
  },

  toJSON(message: ConfigSubscription): unknown {
    const obj: any = {};
    if (message.start !== undefined) {
      obj.start = ConfigSubscriptionStart.toJSON(message.start);
    }
    if (message.syncDone !== undefined) {
      obj.syncDone = ConfigSubscriptionSyncDone.toJSON(message.syncDone);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSubscription>, I>>(base?: I): ConfigSubscription {
    return ConfigSubscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSubscription>, I>>(object: I): ConfigSubscription {
    const message = createBaseConfigSubscription();
    message.start = (object.start !== undefined && object.start !== null)
      ? ConfigSubscriptionStart.fromPartial(object.start)
      : undefined;
    message.syncDone = (object.syncDone !== undefined && object.syncDone !== null)
      ? ConfigSubscriptionSyncDone.fromPartial(object.syncDone)
      : undefined;
    return message;
  },
};

function createBaseConfigSubscriptionStart(): ConfigSubscriptionStart {
  return {};
}

export const ConfigSubscriptionStart: MessageFns<ConfigSubscriptionStart> = {
  encode(_: ConfigSubscriptionStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSubscriptionStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSubscriptionStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ConfigSubscriptionStart {
    return {};
  },

  toJSON(_: ConfigSubscriptionStart): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSubscriptionStart>, I>>(base?: I): ConfigSubscriptionStart {
    return ConfigSubscriptionStart.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSubscriptionStart>, I>>(_: I): ConfigSubscriptionStart {
    const message = createBaseConfigSubscriptionStart();
    return message;
  },
};

function createBaseConfigSubscriptionSyncDone(): ConfigSubscriptionSyncDone {
  return { commitConfirmId: "", serverCommitId: "", done: false };
}

export const ConfigSubscriptionSyncDone: MessageFns<ConfigSubscriptionSyncDone> = {
  encode(message: ConfigSubscriptionSyncDone, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitConfirmId !== "") {
      writer.uint32(10).string(message.commitConfirmId);
    }
    if (message.serverCommitId !== "") {
      writer.uint32(18).string(message.serverCommitId);
    }
    if (message.done !== false) {
      writer.uint32(24).bool(message.done);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfigSubscriptionSyncDone {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigSubscriptionSyncDone();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitConfirmId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.serverCommitId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.done = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfigSubscriptionSyncDone {
    return {
      commitConfirmId: isSet(object.commitConfirmId)
        ? globalThis.String(object.commitConfirmId)
        : isSet(object.commit_confirm_id)
        ? globalThis.String(object.commit_confirm_id)
        : "",
      serverCommitId: isSet(object.serverCommitId)
        ? globalThis.String(object.serverCommitId)
        : isSet(object.server_commit_id)
        ? globalThis.String(object.server_commit_id)
        : "",
      done: isSet(object.done) ? globalThis.Boolean(object.done) : false,
    };
  },

  toJSON(message: ConfigSubscriptionSyncDone): unknown {
    const obj: any = {};
    if (message.commitConfirmId !== "") {
      obj.commitConfirmId = message.commitConfirmId;
    }
    if (message.serverCommitId !== "") {
      obj.serverCommitId = message.serverCommitId;
    }
    if (message.done !== false) {
      obj.done = message.done;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfigSubscriptionSyncDone>, I>>(base?: I): ConfigSubscriptionSyncDone {
    return ConfigSubscriptionSyncDone.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfigSubscriptionSyncDone>, I>>(object: I): ConfigSubscriptionSyncDone {
    const message = createBaseConfigSubscriptionSyncDone();
    message.commitConfirmId = object.commitConfirmId ?? "";
    message.serverCommitId = object.serverCommitId ?? "";
    message.done = object.done ?? false;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
